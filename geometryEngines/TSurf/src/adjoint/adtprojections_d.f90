!        Generated by TAPENADE     (INRIA, Tropics team)
!  Tapenade 3.10 (r5363) -  9 Sep 2014 09:53
!
!
!     ******************************************************************
!     *                                                                *
!     * File:          adtProjections.F90                              *
!     * Author:        Edwin van der Weide                             *
!     * Starting date: 10-27-2016                                      *
!     * Last modified: 10-27-2016                                      *
!     *                                                                *
!     ******************************************************************
!
MODULE ADTPROJECTIONS_D
!
!     ******************************************************************
!     *                                                                *
!     * Module, which contains small subroutines which perform         *
!     * projection tasks. They are grouped here so they could be       *
!     * easily differentiated.                                         *
!     *                                                                *
!     ******************************************************************
!
  USE PRECISION
  USE CONSTANTS
  IMPLICIT NONE

CONTAINS
!  Differentiation of triaprojection in forward (tangent) mode:
!   variations   of useful results: u v xf
!   with respect to varying inputs: x x1 x2 x3
!   RW status of diff variables: u:out v:out x:in xf:out x1:in
!                x2:in x3:in
!===============================================================
  SUBROUTINE TRIAPROJECTION_D(x1, x1d, x2, x2d, x3, x3d, x, xd, xf, xfd&
&   , u, ud, v, vd, val)
    IMPLICIT NONE
! DECLARATIONS
! Input variables
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x1, x2, x3
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x1d, x2d, x3d
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: xd
! Output variables
    REAL(kind=realtype), DIMENSION(3), INTENT(OUT) :: xf
    REAL(kind=realtype), DIMENSION(3), INTENT(OUT) :: xfd
    REAL(kind=realtype), INTENT(OUT) :: u, v, val
    REAL(kind=realtype), INTENT(OUT) :: ud, vd
! Working variables
    REAL(kind=realtype), DIMENSION(3) :: a, b, vf, vt
    REAL(kind=realtype), DIMENSION(3) :: ad, bd, vfd, vtd
    REAL(kind=realtype), DIMENSION(3) :: an, bn, norm
    REAL(kind=realtype), DIMENSION(3) :: and, bnd, normd
    REAL(kind=realtype) :: invlen, vn, dx, dy, dz, uv
    REAL(kind=realtype) :: invlend, vnd, uvd
    INTRINSIC SQRT
    INTRINSIC MAX
    INTRINSIC ABS
    INTRINSIC SIGN
    INTRINSIC MIN
    REAL(kind=realtype) :: arg1
    REAL(kind=realtype) :: arg1d
    REAL(kind=realtype) :: y4d
    REAL(kind=realtype) :: max2d
    REAL(kind=realtype) :: y3d
    REAL(kind=realtype) :: max1d
    REAL(kind=realtype) :: y2d
    REAL(kind=realtype) :: y5d
    REAL(kind=realtype) :: max3d
    REAL(kind=realtype) :: max3
    REAL(kind=realtype) :: max2
    REAL(kind=realtype) :: max1
    REAL(kind=realtype) :: y5
    REAL(kind=realtype) :: y4
    REAL(kind=realtype) :: y3
    REAL(kind=realtype) :: y2
    REAL(kind=realtype) :: y1
    REAL(kind=realtype) :: y1d
! EXECUTION
! Determine the tangent vectors in u- and v-direction.
! Store these in a and b respectively.
    ad = x2d - x1d
    a = x2 - x1
    bd = x3d - x1d
    b = x3 - x1
! Determine the normal vector of the face by taking the
! cross product of a and b. Afterwards this vector will
! be scaled to a unit vector.
    normd = 0.0
    normd(1) = ad(2)*b(3) + a(2)*bd(3) - ad(3)*b(2) - a(3)*bd(2)
    norm(1) = a(2)*b(3) - a(3)*b(2)
    normd(2) = ad(3)*b(1) + a(3)*bd(1) - ad(1)*b(3) - a(1)*bd(3)
    norm(2) = a(3)*b(1) - a(1)*b(3)
    normd(3) = ad(1)*b(2) + a(1)*bd(2) - ad(2)*b(1) - a(2)*bd(1)
    norm(3) = a(1)*b(2) - a(2)*b(1)
    arg1d = normd(1)*norm(1) + norm(1)*normd(1) + normd(2)*norm(2) + &
&     norm(2)*normd(2) + normd(3)*norm(3) + norm(3)*normd(3)
    arg1 = norm(1)*norm(1) + norm(2)*norm(2) + norm(3)*norm(3)
    IF (arg1 .EQ. 0.0) THEN
      y1d = 0.0
    ELSE
      y1d = arg1d/(2.0*SQRT(arg1))
    END IF
    y1 = SQRT(arg1)
    IF (eps .LT. y1) THEN
      max1d = y1d
      max1 = y1
    ELSE
      max1 = eps
      max1d = 0.0
    END IF
    invlend = -(one*max1d/max1**2)
    invlen = one/max1
    normd(1) = normd(1)*invlen + norm(1)*invlend
    norm(1) = norm(1)*invlen
    normd(2) = normd(2)*invlen + norm(2)*invlend
    norm(2) = norm(2)*invlen
    normd(3) = normd(3)*invlen + norm(3)*invlend
    norm(3) = norm(3)*invlen
! Determine the vector vf from xf to given coordinate.
    vfd = 0.0
    vfd(1) = xd(1) - x1d(1)
    vf(1) = x(1) - x1(1)
    vfd(2) = xd(2) - x1d(2)
    vf(2) = x(2) - x1(2)
    vfd(3) = xd(3) - x1d(3)
    vf(3) = x(3) - x1(3)
! Determine the projection of the vector vf onto
! the face.
    vnd = vfd(1)*norm(1) + vf(1)*normd(1) + vfd(2)*norm(2) + vf(2)*normd&
&     (2) + vfd(3)*norm(3) + vf(3)*normd(3)
    vn = vf(1)*norm(1) + vf(2)*norm(2) + vf(3)*norm(3)
    vtd = 0.0
    vtd(1) = vfd(1) - vnd*norm(1) - vn*normd(1)
    vt(1) = vf(1) - vn*norm(1)
    vtd(2) = vfd(2) - vnd*norm(2) - vn*normd(2)
    vt(2) = vf(2) - vn*norm(2)
    vtd(3) = vfd(3) - vnd*norm(3) - vn*normd(3)
    vt(3) = vf(3) - vn*norm(3)
! The vector vt points from the current point on the
! face to the new point. However this new point lies on
! the plane determined by the vectors a and b, but not
! necessarily on the face itself. The new point on the
! face is obtained by projecting the point in the a-b
! plane onto the face. this can be done by determining
! the coefficients du and dv, such that vt = du*a + dv*b.
! To solve du and dv the vectors normal to a and b
! inside the plane ab are needed.
    and = 0.0
    and(1) = ad(2)*norm(3) + a(2)*normd(3) - ad(3)*norm(2) - a(3)*normd(&
&     2)
    an(1) = a(2)*norm(3) - a(3)*norm(2)
    and(2) = ad(3)*norm(1) + a(3)*normd(1) - ad(1)*norm(3) - a(1)*normd(&
&     3)
    an(2) = a(3)*norm(1) - a(1)*norm(3)
    and(3) = ad(1)*norm(2) + a(1)*normd(2) - ad(2)*norm(1) - a(2)*normd(&
&     1)
    an(3) = a(1)*norm(2) - a(2)*norm(1)
    bnd = 0.0
    bnd(1) = bd(2)*norm(3) + b(2)*normd(3) - bd(3)*norm(2) - b(3)*normd(&
&     2)
    bn(1) = b(2)*norm(3) - b(3)*norm(2)
    bnd(2) = bd(3)*norm(1) + b(3)*normd(1) - bd(1)*norm(3) - b(1)*normd(&
&     3)
    bn(2) = b(3)*norm(1) - b(1)*norm(3)
    bnd(3) = bd(1)*norm(2) + b(1)*normd(2) - bd(2)*norm(1) - b(2)*normd(&
&     1)
    bn(3) = b(1)*norm(2) - b(2)*norm(1)
! Solve parametric coordinates u and v.
! The clipping of vn should not be
! active, as this would mean that the vectors a and b
! are parallel. This corresponds to a tria degenerated
! to a line, which should not occur in the surface mesh.
    vnd = ad(1)*bn(1) + a(1)*bnd(1) + ad(2)*bn(2) + a(2)*bnd(2) + ad(3)*&
&     bn(3) + a(3)*bnd(3)
    vn = a(1)*bn(1) + a(2)*bn(2) + a(3)*bn(3)
    IF (vn .GE. 0.) THEN
      y2d = vnd
      y2 = vn
    ELSE
      y2d = -vnd
      y2 = -vn
    END IF
    IF (eps .LT. y2) THEN
      max2d = y2d
      max2 = y2
    ELSE
      max2 = eps
      max2d = 0.0
    END IF
    vnd = max2d*SIGN(1.d0, max2*vn)
    vn = SIGN(max2, vn)
    ud = ((vtd(1)*bn(1)+vt(1)*bnd(1)+vtd(2)*bn(2)+vt(2)*bnd(2)+vtd(3)*bn&
&     (3)+vt(3)*bnd(3))*vn-(vt(1)*bn(1)+vt(2)*bn(2)+vt(3)*bn(3))*vnd)/vn&
&     **2
    u = (vt(1)*bn(1)+vt(2)*bn(2)+vt(3)*bn(3))/vn
    vnd = bd(1)*an(1) + b(1)*and(1) + bd(2)*an(2) + b(2)*and(2) + bd(3)*&
&     an(3) + b(3)*and(3)
    vn = b(1)*an(1) + b(2)*an(2) + b(3)*an(3)
    IF (vn .GE. 0.) THEN
      y3d = vnd
      y3 = vn
    ELSE
      y3d = -vnd
      y3 = -vn
    END IF
    IF (eps .LT. y3) THEN
      max3d = y3d
      max3 = y3
    ELSE
      max3 = eps
      max3d = 0.0
    END IF
    vnd = max3d*SIGN(1.d0, max3*vn)
    vn = SIGN(max3, vn)
    vd = ((vtd(1)*an(1)+vt(1)*and(1)+vtd(2)*an(2)+vt(2)*and(2)+vtd(3)*an&
&     (3)+vt(3)*and(3))*vn-(vt(1)*an(1)+vt(2)*an(2)+vt(3)*an(3))*vnd)/vn&
&     **2
    v = (vt(1)*an(1)+vt(2)*an(2)+vt(3)*an(3))/vn
! Triangles should be bounded by the line u + v = 1
    uvd = ud + vd
    uv = u + v
    IF (uv .GT. one) THEN
      ud = (ud*uv-u*uvd)/uv**2
      u = u/uv
      vd = (vd*uv-v*uvd)/uv**2
      v = v/uv
    END IF
    IF (zero .LT. u) THEN
      y4d = ud
      y4 = u
    ELSE
      y4 = zero
      y4d = 0.0
    END IF
    IF (one .GT. y4) THEN
      ud = y4d
      u = y4
    ELSE
      u = one
      ud = 0.0
    END IF
    IF (zero .LT. v) THEN
      y5d = vd
      y5 = v
    ELSE
      y5 = zero
      y5d = 0.0
    END IF
    IF (one .GT. y5) THEN
      vd = y5d
      v = y5
    ELSE
      v = one
      vd = 0.0
    END IF
! Determine the new coordinates of the point xf.
    xfd = x1d + ud*a + u*ad + vd*b + v*bd
    xf = x1 + u*a + v*b
! Compute the distance squared between the given
! coordinate and the point xf.
    dx = x(1) - xf(1)
    dy = x(2) - xf(2)
    dz = x(3) - xf(3)
    val = dx*dx + dy*dy + dz*dz
  END SUBROUTINE TRIAPROJECTION_D
!===============================================================
  SUBROUTINE TRIAPROJECTION(x1, x2, x3, x, xf, u, v, val)
    IMPLICIT NONE
! DECLARATIONS
! Input variables
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x1, x2, x3
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x
! Output variables
    REAL(kind=realtype), DIMENSION(3), INTENT(OUT) :: xf
    REAL(kind=realtype), INTENT(OUT) :: u, v, val
! Working variables
    REAL(kind=realtype), DIMENSION(3) :: a, b, vf, vt
    REAL(kind=realtype), DIMENSION(3) :: an, bn, norm
    REAL(kind=realtype) :: invlen, vn, dx, dy, dz, uv
    INTRINSIC SQRT
    INTRINSIC MAX
    INTRINSIC ABS
    INTRINSIC SIGN
    INTRINSIC MIN
    REAL(kind=realtype) :: arg1
    REAL(kind=realtype) :: max3
    REAL(kind=realtype) :: max2
    REAL(kind=realtype) :: max1
    REAL(kind=realtype) :: y5
    REAL(kind=realtype) :: y4
    REAL(kind=realtype) :: y3
    REAL(kind=realtype) :: y2
    REAL(kind=realtype) :: y1
! EXECUTION
! Determine the tangent vectors in u- and v-direction.
! Store these in a and b respectively.
    a = x2 - x1
    b = x3 - x1
! Determine the normal vector of the face by taking the
! cross product of a and b. Afterwards this vector will
! be scaled to a unit vector.
    norm(1) = a(2)*b(3) - a(3)*b(2)
    norm(2) = a(3)*b(1) - a(1)*b(3)
    norm(3) = a(1)*b(2) - a(2)*b(1)
    arg1 = norm(1)*norm(1) + norm(2)*norm(2) + norm(3)*norm(3)
    y1 = SQRT(arg1)
    IF (eps .LT. y1) THEN
      max1 = y1
    ELSE
      max1 = eps
    END IF
    invlen = one/max1
    norm(1) = norm(1)*invlen
    norm(2) = norm(2)*invlen
    norm(3) = norm(3)*invlen
! Determine the vector vf from xf to given coordinate.
    vf(1) = x(1) - x1(1)
    vf(2) = x(2) - x1(2)
    vf(3) = x(3) - x1(3)
! Determine the projection of the vector vf onto
! the face.
    vn = vf(1)*norm(1) + vf(2)*norm(2) + vf(3)*norm(3)
    vt(1) = vf(1) - vn*norm(1)
    vt(2) = vf(2) - vn*norm(2)
    vt(3) = vf(3) - vn*norm(3)
! The vector vt points from the current point on the
! face to the new point. However this new point lies on
! the plane determined by the vectors a and b, but not
! necessarily on the face itself. The new point on the
! face is obtained by projecting the point in the a-b
! plane onto the face. this can be done by determining
! the coefficients du and dv, such that vt = du*a + dv*b.
! To solve du and dv the vectors normal to a and b
! inside the plane ab are needed.
    an(1) = a(2)*norm(3) - a(3)*norm(2)
    an(2) = a(3)*norm(1) - a(1)*norm(3)
    an(3) = a(1)*norm(2) - a(2)*norm(1)
    bn(1) = b(2)*norm(3) - b(3)*norm(2)
    bn(2) = b(3)*norm(1) - b(1)*norm(3)
    bn(3) = b(1)*norm(2) - b(2)*norm(1)
! Solve parametric coordinates u and v.
! The clipping of vn should not be
! active, as this would mean that the vectors a and b
! are parallel. This corresponds to a tria degenerated
! to a line, which should not occur in the surface mesh.
    vn = a(1)*bn(1) + a(2)*bn(2) + a(3)*bn(3)
    IF (vn .GE. 0.) THEN
      y2 = vn
    ELSE
      y2 = -vn
    END IF
    IF (eps .LT. y2) THEN
      max2 = y2
    ELSE
      max2 = eps
    END IF
    vn = SIGN(max2, vn)
    u = (vt(1)*bn(1)+vt(2)*bn(2)+vt(3)*bn(3))/vn
    vn = b(1)*an(1) + b(2)*an(2) + b(3)*an(3)
    IF (vn .GE. 0.) THEN
      y3 = vn
    ELSE
      y3 = -vn
    END IF
    IF (eps .LT. y3) THEN
      max3 = y3
    ELSE
      max3 = eps
    END IF
    vn = SIGN(max3, vn)
    v = (vt(1)*an(1)+vt(2)*an(2)+vt(3)*an(3))/vn
! Triangles should be bounded by the line u + v = 1
    uv = u + v
    IF (uv .GT. one) THEN
      u = u/uv
      v = v/uv
    END IF
    IF (zero .LT. u) THEN
      y4 = u
    ELSE
      y4 = zero
    END IF
    IF (one .GT. y4) THEN
      u = y4
    ELSE
      u = one
    END IF
    IF (zero .LT. v) THEN
      y5 = v
    ELSE
      y5 = zero
    END IF
    IF (one .GT. y5) THEN
      v = y5
    ELSE
      v = one
    END IF
! Determine the new coordinates of the point xf.
    xf = x1 + u*a + v*b
! Compute the distance squared between the given
! coordinate and the point xf.
    dx = x(1) - xf(1)
    dy = x(2) - xf(2)
    dz = x(3) - xf(3)
    val = dx*dx + dy*dy + dz*dz
  END SUBROUTINE TRIAPROJECTION
!***************************************************************
!***************************************************************
  SUBROUTINE QUADPROJECTION(x1, x2, x3, x4, x, xf, u, v, val)
    IMPLICIT NONE
! DECLARATIONS
! Input variables
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x1, x2, x3, x4
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x
! Output variables
    REAL(kind=realtype), DIMENSION(3), INTENT(OUT) :: xf
    REAL(kind=realtype), INTENT(OUT) :: u, v, val
! Working variables
    REAL(kind=realtype), DIMENSION(3) :: x21, x41, x3142, a, b
    REAL(kind=realtype) :: u_old, v_old, du, dv, error, uv
    REAL(kind=realtype) :: dx, dy, dz
    INTEGER(kind=inttype) :: ll
! Local parameters used in the Newton algorithm.
    INTEGER(kind=inttype), PARAMETER :: itermax=15
    REAL(kind=realtype), PARAMETER :: thresconv=1.e-10_realType
    INTRINSIC MAX
    INTRINSIC MIN
    INTRINSIC SQRT
    REAL(kind=realtype) :: result1
    REAL(kind=realtype) :: y2
    REAL(kind=realtype) :: y1
! EXECUTION
! Initialize u and v to 0.5 and determine the
! corresponding coordinates on the face, which is the
! centroid.
    u = half
    v = half
! Newton loop to determine the point on the surface,
! which minimizes the distance to the given coordinate.
newtonquads:DO ll=1,itermax
! Store previous parametric coordinates
      u_old = u
      v_old = v
      CALL QUADPROJSUBITER(x1, x2, x3, x4, x, u, v, du, dv, error, xf)
! Determine the new parameter values uu and vv. These
! are limited to 0 <= (uu,vv) <= 1.
      u = u + du
      IF (zero .LT. u) THEN
        y1 = u
      ELSE
        y1 = zero
      END IF
      IF (one .GT. y1) THEN
        u = y1
      ELSE
        u = one
      END IF
      v = v + dv
      IF (zero .LT. v) THEN
        y2 = v
      ELSE
        y2 = zero
      END IF
      IF (one .GT. y2) THEN
        v = y2
      ELSE
        v = one
      END IF
! Update error metric (after the cropping)
      du = u - u_old
      dv = v - v_old
      error = du*du + dv*dv
! Exit the loop if the update of the parametric
! weights is below the threshold
      result1 = SQRT(error)
      IF (result1 .LE. thresconv) GOTO 100
    END DO newtonquads
! Call projection one more time for the updated value of u and v
 100 CALL QUADPROJSUBITER(x1, x2, x3, x4, x, u, v, du, dv, error, xf)
! Compute the distance squared between the given
! coordinate and the point xf.
    dx = x(1) - xf(1)
    dy = x(2) - xf(2)
    dz = x(3) - xf(3)
    val = dx*dx + dy*dy + dz*dz
  END SUBROUTINE QUADPROJECTION
  SUBROUTINE QUADPROJSUBITER(x1, x2, x3, x4, x, u, v, du, dv, error, xf)
    IMPLICIT NONE
! DECLARATIONS
! Input variables
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x1, x2, x3, x4
    REAL(kind=realtype), DIMENSION(3), INTENT(IN) :: x
    REAL(kind=realtype), INTENT(IN) :: u, v
! Output variables
    REAL(kind=realtype), INTENT(OUT) :: du, dv, error
    REAL(kind=realtype), DIMENSION(3), INTENT(OUT) :: xf
! Working variables
    REAL(kind=realtype), DIMENSION(3) :: x21, x41, x3142
    REAL(kind=realtype), DIMENSION(3) :: vf, a, b, vt, norm
    REAL(kind=realtype), DIMENSION(3) :: an, bn
    REAL(kind=realtype) :: uv, invlen, vn
    INTRINSIC SQRT
    INTRINSIC MAX
    INTRINSIC ABS
    INTRINSIC SIGN
    REAL(kind=realtype) :: arg1
    REAL(kind=realtype) :: max3
    REAL(kind=realtype) :: max2
    REAL(kind=realtype) :: max1
    REAL(kind=realtype) :: y3
    REAL(kind=realtype) :: y2
    REAL(kind=realtype) :: y1
! EXECUTION
! Determine auxiliary vectors
    x21 = x2 - x1
    x41 = x4 - x1
    x3142 = x3 - x1 - x21 - x41
! Compute guess for projection point
    uv = u*v
    xf = x1 + u*x21 + v*x41 + uv*x3142
! Determine the vector vf from xf to given coordinate.
    vf = x - xf
! Determine the tangent vectors in u- and v-direction.
! Store these in a and b respectively.
    a = x21 + v*x3142
    b = x41 + u*x3142
! Determine the normal vector of the face by taking the
! cross product of a and b. Afterwards this vector will
! be scaled to a unit vector.
    norm(1) = a(2)*b(3) - a(3)*b(2)
    norm(2) = a(3)*b(1) - a(1)*b(3)
    norm(3) = a(1)*b(2) - a(2)*b(1)
    arg1 = norm(1)*norm(1) + norm(2)*norm(2) + norm(3)*norm(3)
    y1 = SQRT(arg1)
    IF (eps .LT. y1) THEN
      max1 = y1
    ELSE
      max1 = eps
    END IF
    invlen = one/max1
    norm(1) = norm(1)*invlen
    norm(2) = norm(2)*invlen
    norm(3) = norm(3)*invlen
! Determine the projection of the vector vf onto
! the face.
    vn = vf(1)*norm(1) + vf(2)*norm(2) + vf(3)*norm(3)
    vt(1) = vf(1) - vn*norm(1)
    vt(2) = vf(2) - vn*norm(2)
    vt(3) = vf(3) - vn*norm(3)
! The vector vt points from the current point on the
! face to the new point. However this new point lies on
! the plane determined by the vectors a and b, but not
! necessarily on the face itself. The new point on the
! face is obtained by projecting the point in the a-b
! plane onto the face. this can be done by determining
! the coefficients du and dv, such that vt = du*a + dv*b.
! To solve du and dv the vectors normal to a and b
! inside the plane ab are needed.
    an(1) = a(2)*norm(3) - a(3)*norm(2)
    an(2) = a(3)*norm(1) - a(1)*norm(3)
    an(3) = a(1)*norm(2) - a(2)*norm(1)
    bn(1) = b(2)*norm(3) - b(3)*norm(2)
    bn(2) = b(3)*norm(1) - b(1)*norm(3)
    bn(3) = b(1)*norm(2) - b(2)*norm(1)
! Solve du and dv. the clipping of vn should not be
! active, as this would mean that the vectors a and b
! are parallel. This corresponds to a quad degenerated
! to a line, which should not occur in the surface mesh.
    vn = a(1)*bn(1) + a(2)*bn(2) + a(3)*bn(3)
    IF (vn .GE. 0.) THEN
      y2 = vn
    ELSE
      y2 = -vn
    END IF
    IF (eps .LT. y2) THEN
      max2 = y2
    ELSE
      max2 = eps
    END IF
    vn = SIGN(max2, vn)
    du = (vt(1)*bn(1)+vt(2)*bn(2)+vt(3)*bn(3))/vn
    vn = b(1)*an(1) + b(2)*an(2) + b(3)*an(3)
    IF (vn .GE. 0.) THEN
      y3 = vn
    ELSE
      y3 = -vn
    END IF
    IF (eps .LT. y3) THEN
      max3 = y3
    ELSE
      max3 = eps
    END IF
    vn = SIGN(max3, vn)
    dv = (vt(1)*an(1)+vt(2)*an(2)+vt(3)*an(3))/vn
! The error is the magnitude of the vector update
    error = du*du + dv*dv
  END SUBROUTINE QUADPROJSUBITER
!  Differentiation of triaweights in forward (tangent) mode:
!   variations   of useful results: weight
!   with respect to varying inputs: u v
!   RW status of diff variables: u:in v:in weight:out
!===============================================================
!===============================================================
!===============================================================
! WEIGHT FUNCTIONS
  SUBROUTINE TRIAWEIGHTS_D(u, ud, v, vd, weight, weightd)
    IMPLICIT NONE
! This subroutine compute the interpolation weights based
! on parametric coordinates.
! DECLARATIONS
! Inputs variables
    REAL(kind=realtype), INTENT(IN) :: u, v
    REAL(kind=realtype), INTENT(IN) :: ud, vd
! Output variables
    REAL(kind=realtype), DIMENSION(8), INTENT(OUT) :: weight
    REAL(kind=realtype), DIMENSION(8), INTENT(OUT) :: weightd
! EXECUTION
! Initialize weight vector
    weight = 0.0
! Update values according to the current element type
    weightd = 0.0
    weightd(1) = -ud - vd
    weight(1) = one - u - v
    weightd(2) = ud
    weight(2) = u
    weightd(3) = vd
    weight(3) = v
  END SUBROUTINE TRIAWEIGHTS_D
!===============================================================
!===============================================================
!===============================================================
! WEIGHT FUNCTIONS
  SUBROUTINE TRIAWEIGHTS(u, v, weight)
    IMPLICIT NONE
! This subroutine compute the interpolation weights based
! on parametric coordinates.
! DECLARATIONS
! Inputs variables
    REAL(kind=realtype), INTENT(IN) :: u, v
! Output variables
    REAL(kind=realtype), DIMENSION(8), INTENT(OUT) :: weight
! EXECUTION
! Initialize weight vector
    weight = 0.0
! Update values according to the current element type
    weight(1) = one - u - v
    weight(2) = u
    weight(3) = v
  END SUBROUTINE TRIAWEIGHTS
!  Differentiation of quadweights in forward (tangent) mode:
!   variations   of useful results: weight
!   with respect to varying inputs: u v
!   RW status of diff variables: u:in v:in weight:out
!===============================================================
  SUBROUTINE QUADWEIGHTS_D(u, ud, v, vd, weight, weightd)
    IMPLICIT NONE
! This subroutine compute the interpolation weights based
! on parametric coordinates.
! DECLARATIONS
! Inputs variables
    REAL(kind=realtype), INTENT(IN) :: u, v
    REAL(kind=realtype), INTENT(IN) :: ud, vd
! Output variables
    REAL(kind=realtype), DIMENSION(8), INTENT(OUT) :: weight
    REAL(kind=realtype), DIMENSION(8), INTENT(OUT) :: weightd
! EXECUTION
! Initialize weight vector
    weight = 0.0
! Update values according to the current element type
    weightd = 0.0
    weightd(1) = -(ud*(one-v)) - (one-u)*vd
    weight(1) = (one-u)*(one-v)
    weightd(2) = ud*(one-v) - u*vd
    weight(2) = u*(one-v)
    weightd(3) = ud*v + u*vd
    weight(3) = u*v
    weightd(4) = (one-u)*vd - ud*v
    weight(4) = (one-u)*v
  END SUBROUTINE QUADWEIGHTS_D
!===============================================================
  SUBROUTINE QUADWEIGHTS(u, v, weight)
    IMPLICIT NONE
! This subroutine compute the interpolation weights based
! on parametric coordinates.
! DECLARATIONS
! Inputs variables
    REAL(kind=realtype), INTENT(IN) :: u, v
! Output variables
    REAL(kind=realtype), DIMENSION(8), INTENT(OUT) :: weight
! EXECUTION
! Initialize weight vector
    weight = 0.0
! Update values according to the current element type
    weight(1) = (one-u)*(one-v)
    weight(2) = u*(one-v)
    weight(3) = u*v
    weight(4) = (one-u)*v
  END SUBROUTINE QUADWEIGHTS
!  Differentiation of computenodalnormals in forward (tangent) mode:
!   variations   of useful results: nodalnormals
!   with respect to varying inputs: coor nodalnormals
!   RW status of diff variables: coor:in nodalnormals:in-out
!===============================================================
!===============================================================
!===============================================================
  SUBROUTINE COMPUTENODALNORMALS_D(ncoor, ntria, nquads, coor, coord, &
&   triaconn, quadsconn, nodalnormals, nodalnormalsd)
    IMPLICIT NONE
!
!       Subroutine arguments.
!
! Input
    INTEGER(kind=inttype), INTENT(IN) :: ncoor, ntria, nquads
    REAL(kind=realtype), DIMENSION(3, ncoor), INTENT(IN) :: coor
    REAL(kind=realtype), DIMENSION(3, ncoor), INTENT(IN) :: coord
    INTEGER(kind=inttype), DIMENSION(3, ntria), INTENT(IN) :: triaconn
    INTEGER(kind=inttype), DIMENSION(4, nquads), INTENT(IN) :: quadsconn
! Output
    REAL(kind=realtype), DIMENSION(3, ncoor), INTENT(OUT) :: &
&   nodalnormals
    REAL(kind=realtype), DIMENSION(3, ncoor), INTENT(OUT) :: &
&   nodalnormalsd
! Working
    REAL(kind=realtype), DIMENSION(ncoor) :: connect_count
    REAL(kind=realtype) :: normal1(3), normal2(3), normal3(3), normal4(3&
&   )
    REAL(kind=realtype) :: normal1d(3), normal2d(3), normal3d(3), &
&   normal4d(3)
    INTEGER(kind=inttype) :: i, ind1, ind2, ind3, ind4
    REAL(kind=realtype) :: x1(3), x2(3), x3(3), x4(3)
    REAL(kind=realtype) :: x1d(3), x2d(3), x3d(3), x4d(3)
    REAL(kind=realtype) :: x12(3), x23(3), x34(3), x41(3)
    REAL(kind=realtype) :: x12d(3), x23d(3), x34d(3), x41d(3)
    REAL(kind=realtype) :: dotresult
    REAL(kind=realtype) :: dotresultd
    INTRINSIC SQRT
    REAL(kind=realtype) :: result1
    REAL(kind=realtype) :: result1d
    normal1d = 0.0
!===============================================================
! Loop over triangle connectivities
    DO i=1,ntria
! Get the indices for each node of the triangle element
      ind1 = triaconn(1, i)
      ind2 = triaconn(2, i)
      ind3 = triaconn(3, i)
! Get the coordinates for each node of the triangle element
      x1d = coord(:, ind1)
      x1 = coor(:, ind1)
      x2d = coord(:, ind2)
      x2 = coor(:, ind2)
      x3d = coord(:, ind3)
      x3 = coor(:, ind3)
! Compute relative vectors for the triangle sides
      x12d = x1d - x2d
      x12 = x1 - x2
      x23d = x2d - x3d
      x23 = x2 - x3
! Take the cross-product of these vectors to obtain the normal vector
      CALL CROSSPROD_D(x12, x12d, x23, x23d, normal1, normal1d)
! Normalize this normal vector
      CALL DOTPROD_D(normal1, normal1d, normal1, normal1d, dotresult, &
&              dotresultd)
      IF (dotresult .EQ. 0.0) THEN
        result1d = 0.0
      ELSE
        result1d = dotresultd/(2.0*SQRT(dotresult))
      END IF
      result1 = SQRT(dotresult)
      normal1d = (normal1d*result1-normal1*result1d)/result1**2
      normal1 = normal1/result1
! Add the contribution of this normal to the nodalNormals array
      nodalnormalsd(:, ind1) = nodalnormalsd(:, ind1) + normal1d
      nodalnormals(:, ind1) = nodalnormals(:, ind1) + normal1
      nodalnormalsd(:, ind2) = nodalnormalsd(:, ind2) + normal1d
      nodalnormals(:, ind2) = nodalnormals(:, ind2) + normal1
      nodalnormalsd(:, ind3) = nodalnormalsd(:, ind3) + normal1d
      nodalnormals(:, ind3) = nodalnormals(:, ind3) + normal1
! Add connectivity information to the connect_count array so we
! know how many edges are connected to a node.
! We divide the nodalNormals vector by this number to obtain the
! averaged nodal normal.
      connect_count(ind1) = connect_count(ind1) + 1
      connect_count(ind2) = connect_count(ind2) + 1
      connect_count(ind3) = connect_count(ind3) + 1
    END DO
    normal2d = 0.0
    normal3d = 0.0
    normal4d = 0.0
! Loop over quad connectivities
    DO i=1,nquads
! Get the indices for each node of the quad element
      ind1 = quadsconn(1, i)
      ind2 = quadsconn(2, i)
      ind3 = quadsconn(3, i)
      ind4 = quadsconn(4, i)
! Get the coordinates for each node of the quad element
      x1d = coord(:, ind1)
      x1 = coor(:, ind1)
      x2d = coord(:, ind2)
      x2 = coor(:, ind2)
      x3d = coord(:, ind3)
      x3 = coor(:, ind3)
      x4d = coord(:, ind4)
      x4 = coor(:, ind4)
! Compute relative vectors for the quad sides
      x12d = x1d - x2d
      x12 = x1 - x2
      x23d = x2d - x3d
      x23 = x2 - x3
      x34d = x3d - x4d
      x34 = x3 - x4
      x41d = x4d - x1d
      x41 = x4 - x1
! Take the cross-product of these vectors to obtain the normal vectors
! Normalize these normal vectors
      CALL CROSSPROD_D(x12, x12d, -x41, -x41d, normal1, normal1d)
      CALL DOTPROD_D(normal1, normal1d, normal1, normal1d, dotresult, &
&              dotresultd)
      IF (dotresult .EQ. 0.0) THEN
        result1d = 0.0
      ELSE
        result1d = dotresultd/(2.0*SQRT(dotresult))
      END IF
      result1 = SQRT(dotresult)
      normal1d = (normal1d*result1-normal1*result1d)/result1**2
      normal1 = normal1/result1
      CALL CROSSPROD_D(x23, x23d, -x12, -x12d, normal2, normal2d)
      CALL DOTPROD_D(normal2, normal2d, normal2, normal2d, dotresult, &
&              dotresultd)
      IF (dotresult .EQ. 0.0) THEN
        result1d = 0.0
      ELSE
        result1d = dotresultd/(2.0*SQRT(dotresult))
      END IF
      result1 = SQRT(dotresult)
      normal2d = (normal2d*result1-normal2*result1d)/result1**2
      normal2 = normal2/result1
      CALL CROSSPROD_D(x34, x34d, -x23, -x23d, normal3, normal3d)
      CALL DOTPROD_D(normal3, normal3d, normal3, normal3d, dotresult, &
&              dotresultd)
      IF (dotresult .EQ. 0.0) THEN
        result1d = 0.0
      ELSE
        result1d = dotresultd/(2.0*SQRT(dotresult))
      END IF
      result1 = SQRT(dotresult)
      normal3d = (normal3d*result1-normal3*result1d)/result1**2
      normal3 = normal3/result1
      CALL CROSSPROD_D(x41, x41d, -x34, -x34d, normal4, normal4d)
      CALL DOTPROD_D(normal4, normal4d, normal4, normal4d, dotresult, &
&              dotresultd)
      IF (dotresult .EQ. 0.0) THEN
        result1d = 0.0
      ELSE
        result1d = dotresultd/(2.0*SQRT(dotresult))
      END IF
      result1 = SQRT(dotresult)
      normal4d = (normal4d*result1-normal4*result1d)/result1**2
      normal4 = normal4/result1
! Add the contribution of this normal to the nodalNormals array
      nodalnormalsd(:, ind1) = nodalnormalsd(:, ind1) + normal1d
      nodalnormals(:, ind1) = nodalnormals(:, ind1) + normal1
      nodalnormalsd(:, ind2) = nodalnormalsd(:, ind2) + normal2d
      nodalnormals(:, ind2) = nodalnormals(:, ind2) + normal2
      nodalnormalsd(:, ind3) = nodalnormalsd(:, ind3) + normal3d
      nodalnormals(:, ind3) = nodalnormals(:, ind3) + normal3
      nodalnormalsd(:, ind4) = nodalnormalsd(:, ind4) + normal4d
      nodalnormals(:, ind4) = nodalnormals(:, ind4) + normal4
! Add connectivity information to the connect_count array so we
! know how many edges are connected to a node.
! We divide the nodalNormals vector by this number to obtain the
! averaged nodal normal.
      connect_count(ind1) = connect_count(ind1) + 1
      connect_count(ind2) = connect_count(ind2) + 1
      connect_count(ind3) = connect_count(ind3) + 1
      connect_count(ind4) = connect_count(ind4) + 1
    END DO
    DO i=1,3
! Divide the nodal normals by the number of edges that each node has
      nodalnormalsd(i, :) = nodalnormalsd(i, :)/connect_count
      nodalnormals(i, :) = nodalnormals(i, :)/connect_count
    END DO
    DO i=1,ncoor
! Normalize these new averaged nodal normals
      normal1d = nodalnormalsd(:, i)
      normal1 = nodalnormals(:, i)
      CALL DOTPROD_D(normal1, normal1d, normal1, normal1d, dotresult, &
&              dotresultd)
      IF (dotresult .EQ. 0.0) THEN
        result1d = 0.0
      ELSE
        result1d = dotresultd/(2.0*SQRT(dotresult))
      END IF
      result1 = SQRT(dotresult)
      nodalnormalsd(:, i) = (normal1d*result1-normal1*result1d)/result1&
&       **2
      nodalnormals(:, i) = normal1/result1
    END DO
  END SUBROUTINE COMPUTENODALNORMALS_D
!===============================================================
!===============================================================
!===============================================================
  SUBROUTINE COMPUTENODALNORMALS(ncoor, ntria, nquads, coor, triaconn, &
&   quadsconn, nodalnormals)
    IMPLICIT NONE
!
!       Subroutine arguments.
!
! Input
    INTEGER(kind=inttype), INTENT(IN) :: ncoor, ntria, nquads
    REAL(kind=realtype), DIMENSION(3, ncoor), INTENT(IN) :: coor
    INTEGER(kind=inttype), DIMENSION(3, ntria), INTENT(IN) :: triaconn
    INTEGER(kind=inttype), DIMENSION(4, nquads), INTENT(IN) :: quadsconn
! Output
    REAL(kind=realtype), DIMENSION(3, ncoor), INTENT(OUT) :: &
&   nodalnormals
! Working
    REAL(kind=realtype), DIMENSION(ncoor) :: connect_count
    REAL(kind=realtype) :: normal1(3), normal2(3), normal3(3), normal4(3&
&   )
    INTEGER(kind=inttype) :: i, ind1, ind2, ind3, ind4
    REAL(kind=realtype) :: x1(3), x2(3), x3(3), x4(3)
    REAL(kind=realtype) :: x12(3), x23(3), x34(3), x41(3)
    REAL(kind=realtype) :: dotresult
    INTRINSIC SQRT
    REAL(kind=realtype) :: result1
!===============================================================
! Loop over triangle connectivities
    DO i=1,ntria
! Get the indices for each node of the triangle element
      ind1 = triaconn(1, i)
      ind2 = triaconn(2, i)
      ind3 = triaconn(3, i)
! Get the coordinates for each node of the triangle element
      x1 = coor(:, ind1)
      x2 = coor(:, ind2)
      x3 = coor(:, ind3)
! Compute relative vectors for the triangle sides
      x12 = x1 - x2
      x23 = x2 - x3
! Take the cross-product of these vectors to obtain the normal vector
      CALL CROSSPROD(x12, x23, normal1)
! Normalize this normal vector
      CALL DOTPROD(normal1, normal1, dotresult)
      result1 = SQRT(dotresult)
      normal1 = normal1/result1
! Add the contribution of this normal to the nodalNormals array
      nodalnormals(:, ind1) = nodalnormals(:, ind1) + normal1
      nodalnormals(:, ind2) = nodalnormals(:, ind2) + normal1
      nodalnormals(:, ind3) = nodalnormals(:, ind3) + normal1
! Add connectivity information to the connect_count array so we
! know how many edges are connected to a node.
! We divide the nodalNormals vector by this number to obtain the
! averaged nodal normal.
      connect_count(ind1) = connect_count(ind1) + 1
      connect_count(ind2) = connect_count(ind2) + 1
      connect_count(ind3) = connect_count(ind3) + 1
    END DO
! Loop over quad connectivities
    DO i=1,nquads
! Get the indices for each node of the quad element
      ind1 = quadsconn(1, i)
      ind2 = quadsconn(2, i)
      ind3 = quadsconn(3, i)
      ind4 = quadsconn(4, i)
! Get the coordinates for each node of the quad element
      x1 = coor(:, ind1)
      x2 = coor(:, ind2)
      x3 = coor(:, ind3)
      x4 = coor(:, ind4)
! Compute relative vectors for the quad sides
      x12 = x1 - x2
      x23 = x2 - x3
      x34 = x3 - x4
      x41 = x4 - x1
! Take the cross-product of these vectors to obtain the normal vectors
! Normalize these normal vectors
      CALL CROSSPROD(x12, -x41, normal1)
      CALL DOTPROD(normal1, normal1, dotresult)
      result1 = SQRT(dotresult)
      normal1 = normal1/result1
      CALL CROSSPROD(x23, -x12, normal2)
      CALL DOTPROD(normal2, normal2, dotresult)
      result1 = SQRT(dotresult)
      normal2 = normal2/result1
      CALL CROSSPROD(x34, -x23, normal3)
      CALL DOTPROD(normal3, normal3, dotresult)
      result1 = SQRT(dotresult)
      normal3 = normal3/result1
      CALL CROSSPROD(x41, -x34, normal4)
      CALL DOTPROD(normal4, normal4, dotresult)
      result1 = SQRT(dotresult)
      normal4 = normal4/result1
! Add the contribution of this normal to the nodalNormals array
      nodalnormals(:, ind1) = nodalnormals(:, ind1) + normal1
      nodalnormals(:, ind2) = nodalnormals(:, ind2) + normal2
      nodalnormals(:, ind3) = nodalnormals(:, ind3) + normal3
      nodalnormals(:, ind4) = nodalnormals(:, ind4) + normal4
! Add connectivity information to the connect_count array so we
! know how many edges are connected to a node.
! We divide the nodalNormals vector by this number to obtain the
! averaged nodal normal.
      connect_count(ind1) = connect_count(ind1) + 1
      connect_count(ind2) = connect_count(ind2) + 1
      connect_count(ind3) = connect_count(ind3) + 1
      connect_count(ind4) = connect_count(ind4) + 1
    END DO
    DO i=1,3
! Divide the nodal normals by the number of edges that each node has
      nodalnormals(i, :) = nodalnormals(i, :)/connect_count
    END DO
    DO i=1,ncoor
! Normalize these new averaged nodal normals
      normal1 = nodalnormals(:, i)
      CALL DOTPROD(normal1, normal1, dotresult)
      result1 = SQRT(dotresult)
      nodalnormals(:, i) = normal1/result1
    END DO
  END SUBROUTINE COMPUTENODALNORMALS
!  Differentiation of crossprod in forward (tangent) mode:
!   variations   of useful results: c
!   with respect to varying inputs: a b c
!===============================================================
!===============================================================
!===============================================================
  SUBROUTINE CROSSPROD_D(a, ad, b, bd, c, cd)
    IMPLICIT NONE
    REAL(kind=realtype), INTENT(IN) :: a(3), b(3)
    REAL(kind=realtype), INTENT(IN) :: ad(3), bd(3)
    REAL(kind=realtype), INTENT(OUT) :: c(3)
    REAL(kind=realtype), INTENT(OUT) :: cd(3)
    cd(1) = ad(2)*b(3) + a(2)*bd(3) - ad(3)*b(2) - a(3)*bd(2)
    c(1) = a(2)*b(3) - a(3)*b(2)
    cd(2) = ad(3)*b(1) + a(3)*bd(1) - ad(1)*b(3) - a(1)*bd(3)
    c(2) = a(3)*b(1) - a(1)*b(3)
    cd(3) = ad(1)*b(2) + a(1)*bd(2) - ad(2)*b(1) - a(2)*bd(1)
    c(3) = a(1)*b(2) - a(2)*b(1)
  END SUBROUTINE CROSSPROD_D
!===============================================================
!===============================================================
!===============================================================
  SUBROUTINE CROSSPROD(a, b, c)
    IMPLICIT NONE
    REAL(kind=realtype), INTENT(IN) :: a(3), b(3)
    REAL(kind=realtype), INTENT(OUT) :: c(3)
    c(1) = a(2)*b(3) - a(3)*b(2)
    c(2) = a(3)*b(1) - a(1)*b(3)
    c(3) = a(1)*b(2) - a(2)*b(1)
  END SUBROUTINE CROSSPROD
!  Differentiation of dotprod in forward (tangent) mode:
!   variations   of useful results: c
!   with respect to varying inputs: a b
!===============================================================
  SUBROUTINE DOTPROD_D(a, ad, b, bd, c, cd)
    IMPLICIT NONE
! This subroutine simply computes a dot product c = a.dot.b
! This function is redundant with the one defined in
! Utilities.F90, but I decided to do it in order to
! keep ADT independent of other pySurf modules.
!
! Ney Secco 2016-10
! DECLARATIONS
! Input variables
    REAL(kind=realtype), DIMENSION(:), INTENT(IN) :: a, b
    REAL(kind=realtype), DIMENSION(:), INTENT(IN) :: ad, bd
! Output variables
    REAL(kind=realtype), INTENT(OUT) :: c
    REAL(kind=realtype), INTENT(OUT) :: cd
! Working variables
    INTEGER(kind=inttype) :: ii
    INTRINSIC SIZE
! EXECUTION
    c = 0.0
    cd = 0.0
    DO ii=1,SIZE(a)
      cd = cd + ad(ii)*b(ii) + a(ii)*bd(ii)
      c = c + a(ii)*b(ii)
    END DO
  END SUBROUTINE DOTPROD_D
!===============================================================
  SUBROUTINE DOTPROD(a, b, c)
    IMPLICIT NONE
! This subroutine simply computes a dot product c = a.dot.b
! This function is redundant with the one defined in
! Utilities.F90, but I decided to do it in order to
! keep ADT independent of other pySurf modules.
!
! Ney Secco 2016-10
! DECLARATIONS
! Input variables
    REAL(kind=realtype), DIMENSION(:), INTENT(IN) :: a, b
! Output variables
    REAL(kind=realtype), INTENT(OUT) :: c
! Working variables
    INTEGER(kind=inttype) :: ii
    INTRINSIC SIZE
! EXECUTION
    c = 0.0
    DO ii=1,SIZE(a)
      c = c + a(ii)*b(ii)
    END DO
  END SUBROUTINE DOTPROD
END MODULE ADTPROJECTIONS_D
